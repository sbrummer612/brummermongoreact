maven
mongoDB external instance is required
Spring Data ReactiveMongoDB
Spring Reactive Web (webflux)
Thymeleaf templates
Spring-boot-starter-data-mongodb-reactive uses Spring Data ReactiveCrudRepository so database tables are created by framework and only generic DAO code need to be written.
Reactive also returns Flux<object> objects to the UI
MVC and Rest Services
